/**
 * Cluster Analysis page — runs DBSCAN on filtered defects and visualises
 * the resulting clusters as a colour-coded scatter plot.
 */
export default function ClusterPage(): import("react/jsx-runtime").JSX.Element;
